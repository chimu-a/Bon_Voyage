<td><%= image_tag post_item.get_image, size: "100x100" %></td>
<td><%= post_item.place %></td>
<td><%= post_item.explanatory_text %></td>
<td>
  <div>
    <i class="fa-solid fa-comment-dots"></i>
    <span id="comment_count"><%= post_item.comments.count %></span>
     <div id="post-comments">
      <%= render "admin/comments/commentslist", post_item: post_item, key: key %>
    </div>
  </div>
</td>