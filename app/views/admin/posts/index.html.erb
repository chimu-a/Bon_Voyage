<h1>Admin::Posts#index</h1>
<p>Find me in app/views/admin/posts/index.html.erb</p>

 <div class='container pt-5'>
  <div class='row'>
    <div class='col-lg'>
<h1>投稿一覧(全<%= @posts.count %>件)</h1></br>
<div class "container " >
  <div class="row m-2">

    <% @posts.each do |post| %>
    <div class="col-lg-3" >
    <div class="">
     <div> <%= link_to image_tag(post.image, width: '200', height: '200'), admin_post_path(post) %>  </div>
       <tr>
        　  <td><%= post.title %></td>
        　</tr>
        　<tr>
        <!--　  <%# @post_tags.each do |tag| %>-->
     　　 <!--<td> <i class="fa-sharp fa-solid fa-tag"></i>-->
        <!--<%#= link_to tag.name,search_tag_path(tag_id: tag.id) %></td>-->

         <p><i class="fa-sharp fa-solid fa-tag"></i>
         <%= link_to post.tags.map(&:name).join(', '),search_tag_path(tag_id: tag.id) %></p>

        　</tr>
    </div>
    </div>
    <% end %>
  </div>
</div>
</div>
    </div>
    </div>
<%#= paginate @posts %>
