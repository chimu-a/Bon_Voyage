<% post_item.comments.each do |comment| %>
<p><%#= image_tag comment.customer.get_profile_image(100,100) %>
  <%= link_to comment.customer.user_name, customer_path(post.customer_id), class: "link text-secondary" %>
  <%= comment.created_at.strftime('%Y/%m/%d') %>
  <p><%= comment.content %>
  <% if comment.customer == current_customer %>
    <%= link_to "削除", post_item_comment_path(post_item.id, comment.id), method: :delete, "data-confirm" => "本当に削除しますか？", class:"btn btn-outline-danger btn-sm p-1", remote: true, data: {"turbolinks" => false} %>
  <% end %>
  </p>
<% end %>
</p>