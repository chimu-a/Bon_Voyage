<%= form_with model: [post_item, comment], local: false do |f| %>
  <%= f.text_area :content, rows: '2', placeholder: "コメントをここに" %>
  <%= f.submit "送信する", class:"btn btn-sm btn-outline-primary" %>
<% end %>